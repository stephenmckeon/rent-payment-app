<h1>Platforms</h1>

<ul>
  <% @platforms.each do |platform| %>
    <li><%= link_to platform.name, platform_path(platform) %></li>
  <% end %>
</ul><br>

<%= link_to "New Platform", new_platform_path %>